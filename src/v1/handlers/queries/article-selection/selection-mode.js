// @flow
export type ESelectionMode = number;

/**
 * Enum which defines the including or excluding semantic of an ArticleSelection
 * @type {{INCLUDING: ESelectionMode, EXCLUDING: ESelectionMode}}
 */
export const SelectionMode = {
  INCLUDING: 0,
  EXCLUDING: 1
}
